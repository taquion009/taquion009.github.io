const trailsNeon = () => {
    var canvas = document.querySelector<HTMLCanvasElement>('.trailsNeon');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    var c = canvas.getContext('2d');
}

export default trailsNeon